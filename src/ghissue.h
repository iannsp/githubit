/** declare the functions to issue **/
// usar sprintf para fazer os comandos e recebendo um numero fixo de parametros.
/*
support to list all issues, issues from a repo, e just one issue
*/
//int    ghIssue(ghin param);
ghout   list(ghin param);
ghout   add(ghin param);
ghout   edit(ghin param);
